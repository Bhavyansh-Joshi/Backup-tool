# Drive Backup Tool

Auto-detects connected drives, copies files with SHA-256 verification,
deduplicates by content hash, and runs integrity checks on backups.

## Requirements
pip install psutil

## Run
python3 backup.py